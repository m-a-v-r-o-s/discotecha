DATABASE_URL="file:./dev.db"

# Door
ADMIN_PASSWORD="changeme"
INVITE_CODE="NOMATTER"

# Stripe (test mode)
STRIPE_SECRET_KEY="sk_test_xxx"
STRIPE_WEBHOOK_SECRET="whsec_xxx"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Analytics (optional). Leave blank to keep analytics off entirely; the
# consent banner already gates it, but no script loads at all until a real
# provider's URL + site id are set here.
NEXT_PUBLIC_ANALYTICS_SCRIPT_URL=""
NEXT_PUBLIC_ANALYTICS_SITE_ID=""
